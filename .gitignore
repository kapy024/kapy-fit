.DS_Store
# Copia de trabajo de credenciales. La anon key vive en config.js, que SÍ se
# versiona: es pública por diseño y RLS (sql/002_rls.sql) es lo que protege.
# La service_role key nunca entra al repo.
config.local.js
.superpowers/
.claude/
.claude-flow/
.worktrees/
# Datos reales del dueño exportados durante la migración de proyecto
# (scripts/migracion/exportar.mjs). Nunca entran al repo público; se borran
# al confirmar la migración.
scripts/migracion/datos/
